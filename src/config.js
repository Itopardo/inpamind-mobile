// ── INPAMIND Mobile — Configuration ──
// Change this URL to your deployed backend when available
// For local development, use your computer's IP address (not localhost)

// When deployed to Render, change to something like:
// export const API_URL = 'https://inpamind-backend.onrender.com';

// For local dev — your PC's IP on the local network:
export const API_URL = 'http://192.168.1.102:3000';

// App version
export const APP_VERSION = '1.0.0';
